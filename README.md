# COM354 Portfolio (GitHub Pages)

This is a simple website for **Meet Patel’s COM354 assignments**.

## Customize it

Open `index.html` and replace:

- Your name (“Your Name”)
- Email (`you@example.com`)
- GitHub + LinkedIn links
- The “About” text

Open `script.js` and update the 9 assignment cards (titles, descriptions, and links).

Optional:

- Put your resume in this folder as `resume.pdf` (or update the link in `index.html`).

## Preview locally

Easiest option:

- In Cursor/VS Code, right-click `index.html` and use a live preview extension, or just open the file in your browser.

## Publish on GitHub Pages

1. Create a GitHub repository (example: `meet1118.github.io`).
2. Add these files to the repo (at the repo root):
   - `index.html`
   - `styles.css`
   - `script.js`
   - `README.md`
   - `resume.pdf` (optional)
3. Push to GitHub.
4. In GitHub: **Settings → Pages**
   - **Build and deployment → Source**: “Deploy from a branch”
   - **Branch**: `main` and `/ (root)`
5. Wait 1–2 minutes, then GitHub will show your site URL in the Pages section.

## Want a custom domain or real contact form?

Tell me what you want and I’ll set it up (custom domain instructions, or a free form backend like Formspree).

